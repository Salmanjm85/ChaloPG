# No environment variables are consumed by this public-interface checkpoint.
# The following names are reserved for the approved future backend.
# Verify integrations and implement the backend before enabling them.
# DATABASE_URL=
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=
# BLOB_READ_WRITE_TOKEN=
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=
# Provider-injected names must be verified against connected integration guidance.
# Never enter real credentials in this example file or commit actual .env files.
